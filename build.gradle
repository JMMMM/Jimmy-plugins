apply plugin: 'groovy'

dependencies {
    compile gradleApi()
    compile localGroovy()
}

task "create-dirs" << {
    sourceSets*.java.srcDirs*.each { it.mkdirs() }
    sourceSets*.resources.srcDirs*.each { it.mkdirs() }
}